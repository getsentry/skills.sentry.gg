{
  "name": "skills-sentry-dev",
  "version": "1.0.0",
  "description": "Proxy service mapping pretty URLs to raw GitHub markdown for skills.sentry.dev",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "pack-sdk": "cd ~/Projects/sentry-javascript/packages/hono && yarn build && npm pack --pack-destination $OLDPWD/vendor/ && cd $OLDPWD && npm install",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@sentry/hono": "file:./vendor/sentry-hono-10.43.0.tgz",
    "hono": "^4.12.7"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "oxfmt": "^0.38.0",
    "oxlint": "^1.53.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
